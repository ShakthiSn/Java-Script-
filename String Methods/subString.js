var para = "Start using ServiceNow's powerful Now Platform to build applications that make work better for your organization.";
var slice=para.slice(25,15);
gs.print(slice);// no output
var substr = para.substring(25,15);
gs.print(substr); //viceNow's 

var slice1=para.slice(-25,15);
gs.print(slice1);// no output
var substr1 = para.substring(-25,15);
gs.print(substr1); //Start using Ser  (- values considered as zero)







